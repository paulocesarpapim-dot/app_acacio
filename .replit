# .replit
run = "npm run build && npm install && cd api && npm install && cd .. && node start.js"
modules = ["nodejs-20"]

[nix]
channel = "stable-24_05"

[[ports]]
localPort = 3000
externalPort = 80
